jobwatch — session progress
===========================

## 2026-09-02 — build session 1

### Done
1.  Repo scaffolding: git init, .gitignore (.env, jobwatch.db, *.log), requirements.txt
    (requests, PyYAML, pytest), .env.example, Python 3.11 venv at .venv/.
2.  textutil.py — HTML-to-text (handles Greenhouse's double-escaped content) and
    normalize() for case/whitespace/unicode-dash-insensitive keyword matching.
3.  filters.py — include / exclude / on-site / flag rules. Word-boundary regexes so
    "intern" does not match "internal" and "vp" does not match "vpc".
    "manager" is excluded except "technical account manager".
4.  sources.py — normalizers + fetchers for greenhouse, lever, ashby, remotive,
    remoteok, hn. 20s timeout, one retry, per-source try/except in collect().
5.  config.py — companies.yaml load/save/add, logging to jobwatch.log, minimal .env reader.
6.  companies.yaml — seeded with the 50 requested companies.
7.  verify.py + `jobwatch.py verify` — probes each slug, tries name-derived variants,
    records what worked or marks the entry `unverified` (runs skip those).
8.  store.py — SQLite (jobwatch.db), table `postings` keyed on url, first_seen/last_seen/
    seen_count. Only URLs never seen before count as new.
9.  digest.py — markdown digest grouped by company.
10. mailer.py — SMTP over STARTTLS (or implicit TLS on port 465), ASCII-only subject.
11. jobwatch.py — CLI: run / run --email / list --days N / add / verify.

### Verification results (2026-09-02): 40/50 companies verified
Corrected slugs found by variant search:
  Neon        ashby/neondatabase   -> ashby/neon
  Cursor      ashby/anysphere      -> ashby/cursor
Corrected by cross-ATS probe (the requested ATS was wrong, not the slug):
  Sentry      greenhouse/sentry              -> ashby/sentry
  Notion      greenhouse/notion              -> ashby/notion
  Temporal    greenhouse/temporaltechnologies-> ashby/temporal
  Snowflake   greenhouse/snowflakecomputing  -> ashby/snowflake
  Airbyte     greenhouse/airbyte             -> ashby/airbyte
  Vannevar    ashby/vannevarlabs             -> greenhouse/vannevarlabs
Still unverified (10) — no public greenhouse/lever/ashby board found:
  Expo, Retool, GitHub, Fly.io, HashiCorp, dbt Labs, Weights & Biases,
  Shield AI, Second Front, Govini
Verified but currently empty: Rebellion Defense (real board, zero open roles).

### Gotcha worth remembering
A 200 from a Greenhouse slug is not proof of identity. The variant "shield" returns a
GitHub Pages demo board ("Copy of Avenger", Beijing) — not Shield AI. verify.py now
requires a guessed variant to prove itself via the /v1/boards/{slug} name (Greenhouse)
or the slug appearing in the posting URL path (Lever/Ashby). Seeded slugs are taken at
face value because many real boards apply through a custom domain, so the apply-URL
host proves nothing.

### Next
- pytest suite (filters + one normalizer per source, from fixtures)
- run.sh wrapper + README (cron 7am + Windows Task Scheduler)
- first real run, show the digest

### Done (continued)
12. tests/ — 71 pytest tests. test_filters.py covers every include keyword, every
    exclusion, the technical-account-manager carve-out, word-boundary edge cases
    (internal != intern, VPC != VP), the on-site-unless-remote rule and all flag
    types. test_normalizers.py covers one normalizer per source against small
    payloads saved from the live APIs in tests/fixtures/.
13. run.sh — cron wrapper: cd's to its own dir, picks the venv interpreter,
    sources .env (cron has almost no environment).
14. README.md — commands, filter semantics, per-source notes, the verification
    results table, cron line for 7:00 AM and the Windows Task Scheduler equivalent.

### First real run (2026-09-02 22:39)
43/43 sources succeeded, zero errors. 8,978 raw postings -> 319 matches, all new.
By source: greenhouse 145, ashby 102, lever 61, hn 10, remoteok 1, remotive 0.
264 marked remote, 81 flagged for manual review.
Second run scanned 8,972 and reported 0 new, so the seen/new logic is correct.

### Two bugs found and fixed by running it for real
- HN thread selection returned the March 2020 thread. Algolia's /search endpoint is
  relevance-ranked and returns 20 arbitrary hits, none recent, so "newest hit"
  was newest-of-an-old-sample. Now queries /search_by_date filtered to the
  whoishiring account, with /search kept as a fallback.
- On Windows stdout is cp1252, so the digest wrote an em dash as byte 0x97 and any
  character outside cp1252 would have raised UnicodeEncodeError *after* the whole
  run completed. main() now reconfigures stdout/stderr to UTF-8 with
  errors="replace".

## STOP POINT — 2026-09-02
Everything in the spec is built, tested and has been run for real once against live
APIs. jobwatch.db holds 319 matched postings, so the next `run` reports only what
appeared since. Nothing is half-finished.

To pick up from here:
1. `.venv/Scripts/python.exe -m pytest`            (expect 71 passed)
2. `.venv/Scripts/python.exe jobwatch.py run`      (expect a short digest)
3. Fill in .env from .env.example and try `run --email` — the SMTP path has been
   exercised only on its failure branch (missing env vars -> clean message, exit 2).
   It has never actually sent mail.
4. Schedule it: see the cron line and the Task Scheduler block in README.md.

Open items, none blocking:
- 10 companies remain unverified (Expo, Retool, GitHub, Fly.io, HashiCorp, dbt Labs,
  Weights & Biases, Shield AI, Second Front, Govini). No public Greenhouse/Lever/
  Ashby board found under any variant. Adding a fourth ATS (Workday, Rippling,
  Ripplematch, Ashby-embedded) would recover some of them.
- The digest is noisy at 319 on a first run because everything is new. Day two will
  be a handful. If it stays noisy, the on-site rule only drops postings that say
  "hybrid"/"onsite" out loud — a location-is-a-city-and-not-remote rule would cut
  more, but that goes beyond the spec and would hide genuinely remote-friendly roles.
- Remotive's software-dev feed currently returns only 18 postings and matched none.
  That is the API's real size right now, not a bug.

## 2026-09-02 (later) — follow-up session

### Done
15. Dropped "ai engineer" / "applied ai engineer" from the include list. At these
    companies they are senior ML roles. Removed 22 of 319 matches.
16. Tiering + `--tier`. Tier 1 = support/solutions/implementation/TAM with no
    seniority word; Tier 2 = FDE + customer engineer; Tier 3 = the rest, including
    a tier-1 title carrying a seniority word. `run --tier` / `list --tier` group by
    tier, `list --only-tier N` filters. Tier is derived from the title at render
    time, so no schema migration was needed.
17. Pushed public: https://github.com/kurmudgen/jobwatch
18. Two filter bugs found by actually reading the tier 1 output:
    - Boilerplate "remote" was rescuing on-site roles. Palantir puts "there are a
      few roles that allow for 'Remote' work on an exceptional basis" in every
      description; that one word cancelled the on-site rule for 62 roles whose
      location reads "(onsite)"/"(hybrid)", two of which reached tier 1. The
      escape hatch is now per field. First attempt at the fix silently did nothing
      because _phrase_re("remote -") collapses to a bare \bremote\b - entries are
      now asserted multi-word and tested.
    - Bare "secret" flagged every Supabase support role as needing a clearance
      ("our globally distributed team is our secret weapon"). It now has to sit
      within a few words of "clearance", or be "top secret".
19. Rebuilt jobwatch.db from scratch (approved) so the stored set matches the
    current rules. 224 matches: Tier 1 = 82, Tier 2 = 66, Tier 3 = 76.

### 105 tests passing.

## STOP POINT — 2026-09-02 (follow-up)
Still outstanding: the email has NEVER been sent. mailer.py has only ever been
exercised on its failure branch. .env does not exist. To finish:
  1. Create .env from .env.example with real SMTP values (Gmail needs an App
     Password, not the account password).
  2. `.venv/Scripts/python.exe jobwatch.py run --email --tier`
     Note: the db is now current, so that run will report 0 new and email an empty
     digest. To send a real one, either wait for tomorrow's postings or delete
     jobwatch.db first to re-baseline.
  3. Only after a real send has landed should the 7 AM schedule be trusted.

## 2026-09-03 — us-remote + dedupe

20. --us-remote filter, on by default for run and list (--no-us-remote disables).
    Location must contain "remote" AND either name the US (United States/USA/US/
    North America/NA/AMER/Americas/state name or code) or name no country at all.
    Only the location is consulted - a description mentioning the US says nothing
    about where the role is based.
    Two ordering details matter: strong US tokens are checked BEFORE the foreign
    list so "Remote - NA, APAC, EMEA" survives; state codes are checked AFTER it
    so "CA-Ontario-Toronto" reads as Canada, not California. State codes match
    case-sensitively on the raw string so "in"/"or"/"ok"/"me"/"hi" are not read
    as Indiana/Oregon/Oklahoma/Maine/Hawaii.
    Validated against all 133 distinct locations in the stored data: every kept
    location is US, every dropped one is genuinely foreign.
21. Dedupe on company + normalized title, keeping the US/AMER copy. ElevenLabs
    posts one req per country (18 rows for one job).
22. 148 tests passing.

Pipeline on live data: 8,9xx scanned -> 224 content matches -> 35 us-remote -> 31
after dedupe. Tier 1: 9, Tier 2: 7, Tier 3: 15.

## STOP POINT — 2026-09-03
jobwatch.db has been DELETED on purpose, so the next `run --email` produces a
full 31-posting digest rather than an empty one. Do not run anything that
repopulates it before that send.

BLOCKED on: .env does not exist yet. Jacob is creating it with real SMTP values.
Once he confirms, run:
    .venv/Scripts/python.exe jobwatch.py run --email --tier
and confirm the mail actually arrives. The email path has still never sent a
real message - it has only ever been exercised on its failure branch. Until a
send lands, the 7 AM schedule is not trustworthy.

## 2026-09-03 (later) — cron dropped, connector delivery

23. Deleted the Vercel project (it auto-imported the new public repo and failed
    every build; a Python CLI is not deployable there).
24. Dropped the scheduled job entirely per Jacob: no cron line, no Task Scheduler
    block, run.sh removed (its only purpose was cron), .env deleted. mailer.py and
    --email remain as a documented optional path, marked never-exercised.
25. Added digest.render_html() and --html.

### The delivery bug that took three sends to pin down
Gmail rewrites every URL in a message body into google.com/url?q=...&ust=1788...
The connector's quoted-printable encoder does not escape "=" as "=3D", so the
decoder reads the "=17" inside "ust=17..." as a QP escape for byte 0x17 and eats
it. Every unix-ms timestamp starts with 17, so this breaks EVERY link, forever.
It affects html href attributes too - an early test seemed to show HTML surviving,
but only because that test's markup was accidentally escaped, so Gmail
auto-linkified plain text and inserted its own anchor after encoding. Sending real
<a href> reproduced the corruption.
Fix: send the digest as a base64 ATTACHMENT with no URLs in the body. Verified:
attachment present, 31 hrefs, zero google.com/url wrappers, zero control bytes.
Python smtplib has neither problem (verified byte-identical round trip, 32/32
URLs intact) - relevant only if SMTP is ever configured.

## STOP POINT — 2026-09-03
Working state. 152 tests passing. jobwatch.db holds the current 31 matches, so
the next run reports only what is genuinely new.

Daily workflow, no credentials and no schedule:
    ask Claude Code: "run jobwatch and send me the digest"
    -> python jobwatch.py run --tier
    -> python jobwatch.py list --days 1 --html
    -> base64 that HTML, send as a text/html attachment, no URLs in the body
    -> re-read the sent message to confirm no google.com/url and no control bytes

Open items:
- 10 companies still unverified (Expo, Retool, GitHub, Fly.io, HashiCorp, dbt Labs,
  Weights & Biases, Shield AI, Second Front, Govini). Adding a 4th ATS would
  recover some.
- Supabase "Support Engineer (EMEA - Weekends)" passes the US-remote filter because
  its location field says "Remote, Global" while the title says EMEA. Filter is
  behaving as specified; the posting is mislabelled at the source.

## 2026-09-03 — USAJOBS source

26. USAJOBS added. 4 searches/poll (2210 + each keyword + RemoteIndicator=True +
    PayGradeLow=11), deduped on URL. USAJOBS_EMAIL/USAJOBS_KEY from .env, in
    .env.example, never committed. Missing creds -> clear error, isolated by
    collect(), reported in the digest's failed-sources list.
27. Federal (USAJOBS) digest section in both markdown and HTML, sorted by salary
    max desc, unpublished salaries last. Not tiered; counted in the total.
28. Schema gained salary_min/salary_max (None for other sources).
29. 178 tests passing.

### Three bugs the pipeline run exposed (none would show in a unit test alone)
- The include list CANNOT gate federal postings. Real 2210 titles are "IT
  Specialist (CUSTSPT)", "(APPSW)", "(INFOSEC)" - no "engineer" anywhere, so the
  private-sector keywords matched 1 of 4 fixture rows and none of the common real
  titles. Would have fetched hundreds and silently dropped nearly all. Fix: for
  source==usajobs the API query is the include gate (filters.FEDERAL_SOURCE);
  exclusions, on-site rule and flags still apply, matched_in="usajobs query".
- Salary was lost through SQLite - store.py had no salary columns, so salary_max
  read back None and the Federal sort silently degraded to alphabetical. Fixed
  with an ADD-only migration (store.LATER_COLUMNS + _migrate). Verified against
  the live 31-row db: all rows preserved, idempotent on re-run.
- "Supervisory" is the federal spelling of a management role and passed the
  manager/director/head-of exclusions. Added to EXCLUDE_TITLE_KEYWORDS.

## STOP POINT — 2026-09-03 (USAJOBS)
BLOCKED, non-urgent: no USAJOBS key, so the source is built but has never run
against the live API. It returns 401 to any request without a real key, so the
response shape comes from the published schema, field access is defensive, and
tests/fixtures/usajobs.json is HAND-BUILT, not captured.
When a key exists (free, https://developer.usajobs.gov):
  1. Put USAJOBS_EMAIL + USAJOBS_KEY in .env
  2. .venv/Scripts/python.exe jobwatch.py run --sources usajobs -v
  3. Re-capture tests/fixtures/usajobs.json from the real response and re-check
     every field name, especially where RemoteIndicator actually lives and
     whether PositionLocationDisplay is present.
Everything else is working: 178 tests, db holds the current private-sector
matches, daily workflow is "run jobwatch and send me the digest".

## 2026-09-03 — Workday source

30. Workday source type. companies.yaml slug is the composite tenant/wdNN/site.
    Discovered from each company's public careers redirect:
      Booz Allen Hamilton  bah/wd1/BAH_Jobs                  verified, 2000 jobs
      Accenture            accenture/wd103/AccentureCareers  verified, 2000 jobs
    AFS has no separate public board (afs.accenture.com does not resolve).
31. "Partner / consulting" digest section (markdown + HTML) for source=workday.
32. config VALID_ATS gained "workday"; verify.py probes it with a POST and skips
    name-derived slug variants (a composite slug has no meaningful variants).
33. 193 tests passing. Workday fixtures are captured LIVE (unlike USAJOBS).

### Verified against the live API, not assumed
- limit caps at 20; 50 and 100 both return zero rows. offset pages correctly.
- searchText is a loose stemmed OR match, NOT a phrase filter. "Claude" -> 977
  hits led by "Cloud Engineer"; "solutions engineer" -> 1053. "Anthropic" -> 2 is
  the only precise one. The searches widen the net; title rules do the filtering.
- The list response has no description. Only rows whose title already matches the
  include list get the extra per-posting detail request.

### Slalom and Deloitte: skipped, with evidence
Neither is Workday; both are Avature, and neither has a searchable listing that
plain requests can use. Recorded unverified in companies.yaml with the reason.
  slalom.wd1/wd3/wd5           -> 401 / 422 / 422
  jobs.slalom.com/wday/cxs/... -> 404 HTML (not a Workday custom domain)
  Avature SearchJobs/feed/     -> 200 but a fixed 20 rows
  keywords|keyword|q|searchText|jobKeyword|searchKeyword|3_66_3|freeText -> all ignored
  jobRecordsPerPage=100/200, jobOffset=20 -> ignored, same 20 rows
  Deloitte SearchJobs HTML ?keywords= -> same 10 jobs
  Slalom SearchJobs HTML       -> 0 job links, JS-rendered
No browser dependency added, per the brief.

### Also fixed
The empty-digest message said "0 matched the filters" when 18 HAD matched and
were all dropped by the US-remote filter. It now reports each stage separately.

## STOP POINT — 2026-09-03 (Workday)
Expect the Partner / consulting section to be EMPTY on most default runs. Both
tenants return matching titles (18 on the first live run) but zero survive
--us-remote, because consulting roles are tied to duty stations (McLean,
Arlington, Fort Sam Houston, Hyderabad, Manila, London). Use --no-us-remote to
see them: 16 matches, 10 carrying clearance flags.
Still outstanding from before: no USAJOBS key, so that source has never run live
and its fixture is hand-built.

## 2026-09-03 — USAJOBS key arrived, source fixed against the live API

34. Key in .env (gitignored). Verified absent from tracked files, git history,
    and jobwatch.log. USAJOBS_EMAIL=jspradlin06@gmail.com.
35. The source authenticated first try but returned ZERO rows. Three assumptions
    carried over from the published schema were wrong:
    - RemoteIndicator=True as a QUERY param is the cause of the zero. Alone it
      returns 41 jobs government-wide, NONE in category 2210 (medical officers,
      patent examiners, attorneys), so 2210 + RemoteIndicator = 0 every time.
      No longer sent; the location rule decides. Flip
      sources.USAJOBS_SEND_REMOTE_INDICATOR to restore it.
      Also: PayGradeLow wants "11"; "GS-11" returns HTTP 400.
    - WhoMayApply is EMPTY on all 100 records measured, so the not-open flag
      could never have fired from it. HiringPathDisplay is the real signal
      ("Open to the public" on 75/100). A non-public path now becomes the
      sentence "Not open to the public." for the flag rules.
    - SecurityClearance is a structured field ("Secret"/"Top Secret"/"Sensitive
      Compartmented Information"/"Not Required"), now used instead of prose.
      194 of 250 filtered postings carry a clearance flag.
    Plus: PositionURI carries ":443", now stripped so one posting cannot be
    keyed under two URLs.
    Correct as written: RemoteIndicator IS in UserArea.Details not the
    descriptor (checking both paid off), and 4-digit fractional-second
    timestamps parse fine.
36. Fixture re-captured LIVE, 4 cases: remote/negotiable, not-open, clearance,
    plain. 200 tests passing.

### Live full run (all sources)
494 matched the keyword filters -> 40 survived us-remote -> 35 after dedupe.
5 new: 4 Federal + 1 Tier 3. Federal top: Treasury IT Specialist (AI) $197,200,
Location Negotiable After Selection.

## STOP POINT — 2026-09-03 (USAJOBS live)
Nothing blocked. All five source types work against live APIs: greenhouse,
lever, ashby, workday, usajobs, plus remotive/remoteok/hn boards.
Only remaining known gap: Slalom + Deloitte (Avature) still skipped - no
searchable listing without a browser.
Daily: "run jobwatch and send me the digest" (connector; attach the HTML file,
never put URLs in the body).
